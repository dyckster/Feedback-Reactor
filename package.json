{
  "name": "feedback_reactor",
  "version": "1.0.0",
  "description": "Application that checks firebase for new feedback, creates Trello ticket for it and sends it all to telegram",
  "main": "index.js",
  "scripts": {
    "build": "cross-env NODE_ENV=production node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yury Dombaev",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "firebase-admin": "^8.12.1",
    "request": "^2.88.2"
  }
}
